
----ui componentization & cleanup & finish implementation:

- use paperjs properly
- object-oriented
- not so many variable references when they're all available in the scene graph

learn:
- how to write components for paperjs????
- how to externalize implementations of "objects" (bubble, scorearea, etc..)???

-----features
 - show dernier pli
 - show a table of rounds for game on sidebar with points in each round
   -- tree with game expand rounds.
 - scrolling log of events:  dedans, capot, messages notifying players of events

- implement an alert for notification to replace scaffold window.alert
- onResize handler to redraw table

- transitions/animations:
    - score update (highlight/fade)
    - introduce rotation in animations
    - employ fade/appear effect

- option for ui to display other players' cards face up

---server side:

- ability to join partie as a player or observer, pick player name
- show that can support 1 or more human players in the same partie

- perhaps:  similar to collabedit:  unique url for a game room that can go back to
- ability for a client to receive a full log of events to reconstruct game state in the event
   that an observer joins a game after it has already started

- ability to leave game (close window) and get back to it
- ability to cancel/stop a game in progress

support multiple games in progress
monitor all games in progress

---javascript tests
